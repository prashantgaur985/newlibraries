
def sum(a,b)
{
   echo "The sum of ${a} and ${b} is ${a+b}" 
}

def sub(a,b)
{
   echo "The subtraction result of ${a} and ${b} is ${a-b}"
}

def mul(a,b)
{
   echo "The product of ${a} and ${b} is ${a*b}" 
}

def div(a,b)
{
   echo "The divison os ${a} and ${b} is ${a/b}"
}
